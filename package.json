{
  "name": "the-platapus-audio-streamer",
  "version": "1.0.0",
  "description": "High-quality LAN audio stream server using FFmpeg and WAV",
  "main": "server.js",
  "bin": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "npx pkg . && npx rcedit the-platapus-audio-streamer-win.exe --set-icon favicon.ico"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "pkg": "^5.8.1",
    "rcedit": "^4.0.1"
  },
  "pkg": {
    "assets": [
      "public/**/*"
    ],
    "targets": [
      "node18-win-x64",
      "node18-linux-x64",
      "node18-macos-x64"
    ],
    "icon": "favicon.ico"
  }
}
